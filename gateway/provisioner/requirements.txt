# The AWS Lambda python3.12 runtime bundles boto3 < 1.43, which does not know
# the AgentCore "inference" gateway-target parameter. Vendor a current boto3 so
# the provisioner can create the bedrock-mantle inference target.
boto3>=1.43.0
